<Window
    ExtendClientAreaTitleBarHeightHint="40"
    ExtendClientAreaToDecorationsHint="True"
    Height="750"
    Icon="avares://Lemon.Toolkit/Assets/lemon-50.png"
    MinHeight="600"
    MinWidth="900"
    Name="shell"
    Title="Lemon.Toolkit"
    Width="1200"
    d:DesignHeight="800"
    d:DesignWidth="1000"
    mc:Ignorable="d"
    x:Class="Lemon.Toolkit.Shells.MainWindow"
    x:DataType="local:MainWindowViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:behaviors="using:Lemon.Toolkit.Behaviors"
    xmlns:converters="using:Lemon.Toolkit.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:handylib="using:Lemon.HandyLib.Logging.Definitions"
    xmlns:lm="https://github.com/NeverMorewd/Lemon.ModuleNavigation"
    xmlns:local="using:Lemon.Toolkit.Shells"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:v="using:Lemon.Toolkit.Views"
    xmlns:vm="using:Lemon.Toolkit.ViewModels"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Window.Resources>
        <converters:BoolToBlurEffectConverter x:Key="BoolToBlurEffectConverter" />
        <converters:LogTypeToBrushConverter x:Key="LogTypeToBrushConverter" />
    </Window.Resources>
    <Window.Styles>
        <Style Selector="Image#WindowIcon">
            <Setter Property="Width" Value="28" />
            <Setter Property="Height" Value="28" />
        </Style>
        <Style Selector="Image#WindowIcon:pointerover">
            <Style.Animations>
                <Animation Duration="0:0:0.5" IterationCount="INFINITE">
                    <KeyFrame Cue="0%">
                        <Setter Property="RotateTransform.Angle" Value="1" />
                    </KeyFrame>
                    <KeyFrame Cue="100%">
                        <Setter Property="RotateTransform.Angle" Value="360" />
                    </KeyFrame>
                </Animation>
            </Style.Animations>
        </Style>
    </Window.Styles>

    <Grid
        Margin="4,4,4,4"
        RowDefinitions="40,*,auto"
        ShowGridLines="False">

        <StackPanel
            Grid.Row="0"
            HorizontalAlignment="Left"
            Margin="2,2,0,0"
            Orientation="Horizontal"
            VerticalAlignment="Top">
            <Image
                Name="WindowIcon"
                Source="avares://Lemon.Toolkit/Assets/lemon-50.png"
                Stretch="UniformToFill" />
            <TextBlock
                FontWeight="SemiBold"
                Foreground="Gray"
                Margin="2,5,0,0"
                Text="Lemon.Toolkit" />
        </StackPanel>

        <!--<GridSplitter
            Grid.Row="2"
            Height="1"
            VerticalAlignment="Top" />-->

        <TabControl
            Effect="{Binding IsProcessing, Converter={StaticResource BoolToBlurEffectConverter}}"
            Grid.Row="1"
            HorizontalAlignment="Stretch"
            IsEnabled="{Binding !IsProcessing}"
            Margin="8"
            Name="tab"
            Padding="20,0,0,0"
            TabStripPlacement="Left"
            Theme="{DynamicResource NavigationTab}"
            lm:NavigationExtension.RegionName="MainTabRegion">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <Grid
                        ColumnDefinitions="*,30"
                        Height="30"
                        Width="250">
                        <TextBlock Text="{Binding}" VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </TabControl.ItemTemplate>
        </TabControl>
        <Expander
            ExpandDirection="Up"
            Grid.Row="2"
            Height="NaN"
            IsExpanded="{Binding ConsoleIsExpanded}"
            Margin="0,0,0,10"
            VerticalAlignment="Bottom">
            <Expander.Header>
                <DockPanel LastChildFill="True">
                    <StackPanel
                        DockPanel.Dock="Left"
                        Orientation="Horizontal"
                        Spacing="5">
                        <Viewbox
                            Height="30"
                            Margin="0,10,0,0"
                            Stretch="Uniform"
                            VerticalAlignment="Center"
                            Width="30">
                            <Canvas
                                Height="400.0"
                                UseLayoutRounding="False"
                                Width="400.0">
                                <Path Classes="Retro" StrokeThickness="2">
                                    <Path.Data>
                                        <PathGeometry Figures="M 50,50 L 250,50 L 250,150 L 50,150 Z" />
                                    </Path.Data>
                                </Path>
                                <Path Classes="Retro" StrokeThickness="2">
                                    <Path.Data>
                                        <PathGeometry Figures="M 100,150 L 200,150 L 180,200 L 120,200 Z" />
                                    </Path.Data>
                                </Path>
                                <Path
                                    Classes="Retro"
                                    Opacity="0.3"
                                    StrokeThickness="1">
                                    <Path.Data>
                                        <PathGeometry Figures="M 50,60 L 250,60 M 50,70 L 250,70 M 50,80 L 250,80 M 50,90 L 250,90 M 50,100 L 250,100 M 50,110 L 250,110 M 50,120 L 250,120 M 50,130 L 250,130 M 50,140 L 250,140" />
                                    </Path.Data>
                                </Path>
                            </Canvas>
                        </Viewbox>
                        <TextBlock
                            FontSize="15"
                            Foreground="{DynamicResource RetroBorderBrush}"
                            Text="Console "
                            VerticalAlignment="Center" />
                        <TextBlock
                            FontSize="15"
                            Foreground="{DynamicResource RetroBorderBrush}"
                            Text="{Binding OutputCount}"
                            VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel
                        DockPanel.Dock="Right"
                        HorizontalAlignment="Right"
                        Margin="0,5,20,0"
                        Orientation="Horizontal"
                        Spacing="5"
                        VerticalAlignment="Top">
                        <Button
                            Classes="Retro"
                            Command="{Binding ExecuteCommand}"
                            CommandParameter="{Binding ElementName=Input, Path=Text}"
                            Content="Enter"
                            IsDefault="True" />
                        <Button
                            Classes="RetroDanger"
                            Command="{Binding ClearOutputCommand}"
                            Content="Clear" />
                        <Button
                            Classes="Retro"
                            Command="{Binding CopyOutputCommand}"
                            Content="Copy" />
                    </StackPanel>
                    <TextBox
                        CaretBlinkInterval="0.3"
                        Classes="Retro"
                        Height="35"
                        HorizontalAlignment="Stretch"
                        Margin="15,2,5,0"
                        Name="Input"
                        VerticalAlignment="Stretch" />
                </DockPanel>
            </Expander.Header>
            <Grid
                Margin="2"
                MinHeight="200"
                Name="ConsoleGrid">
                <Border
                    BorderBrush="{DynamicResource RetroBorderBrush}"
                    BorderThickness="1"
                    Margin="0">
                    <Border.Effect>
                        <BlurEffect Radius="8" />
                    </Border.Effect>
                </Border>
                <ListBox
                    ItemsSource="{Binding LogEntries}"
                    Margin="5"
                    MaxHeight="400">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type handylib:LogEntry}">
                            <Grid Height="20" Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="220" />
                                    <!--  Timestamp  -->
                                    <ColumnDefinition Width="50" />
                                    <!--  Level  -->
                                    <ColumnDefinition Width="70" />
                                    <!--  ProcessId  -->
                                    <ColumnDefinition Width="30" />
                                    <!--  ThreadId  -->
                                    <ColumnDefinition Width="100" />
                                    <!--  Interval.Graph  -->
                                    <ColumnDefinition Width="60" />
                                    <!--  Interval  -->
                                    <ColumnDefinition Width="100" />
                                    <!--  Caller  -->
                                    <ColumnDefinition Width="*" />
                                    <!--  Message + Exception  -->
                                    <ColumnDefinition Width="Auto" />
                                    <!--  Expand Button  -->
                                </Grid.ColumnDefinitions>

                                <!--  Timestamp  -->
                                <TextBlock
                                    Classes="Retro"
                                    Grid.Column="0"
                                    Text="{Binding Timestamp, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss.fff}'}" />

                                <!--  Level  -->
                                <TextBlock
                                    Classes="Retro"
                                    Grid.Column="1"
                                    Margin="3,0"
                                    Text="{Binding Level}" />

                                <!--  ProcessId  -->
                                <TextBlock
                                    Classes="Retro"
                                    Grid.Column="2"
                                    Margin="3,0"
                                    Text="{Binding ProcessId}" />

                                <!--  ThreadId  -->
                                <TextBlock
                                    Classes="Retro"
                                    Grid.Column="3"
                                    Margin="3,0"
                                    Text="{Binding ThreadId}" />

                                <!--  Interval  -->
                                <TextBlock
                                    Classes="Retro"
                                    FontFamily="Consolas"
                                    Grid.Column="4"
                                    Margin="3,0"
                                    Text="{Binding IntervalGraph}" />

                                <TextBlock
                                    Classes="Retro"
                                    FontFamily="Consolas"
                                    Grid.Column="5"
                                    Margin="3,0"
                                    MaxWidth="60"
                                    Text="{Binding Interval}"
                                    TextTrimming="CharacterEllipsis"
                                    ToolTip.Tip="{Binding Interval}" />

                                <!--  Caller  -->
                                <TextBlock
                                    Classes="Retro"
                                    Grid.Column="6"
                                    Margin="3,0"
                                    Text="{Binding Caller}"
                                    TextTrimming="CharacterEllipsis"
                                    ToolTip.Tip="{Binding Caller}" />

                                <!--  Message + Exception  -->
                                <TextBlock
                                    Classes="Retro"
                                    Grid.Column="7"
                                    Margin="3,0"
                                    Text="{Binding Message}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap"
                                    ToolTip.Tip="{Binding Message}" />

                                <!--  Expand Button  -->
                                <Button
                                    Classes="Retro"
                                    Command="{Binding $parent[Window].DataContext.ExpandCommand}"
                                    CommandParameter="{Binding}"
                                    Content="Expand"
                                    Grid.Column="8"
                                    Margin="10,0" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.Styles>
                        <Style Selector="TextBlock.Retro" x:DataType="handylib:LogEntry">
                            <Setter Property="Foreground" Value="{Binding Type, Converter={StaticResource LogTypeToBrushConverter}}" />
                        </Style>
                    </ListBox.Styles>
                    <Interaction.Behaviors>
                        <behaviors:ItemsControlAutoScrollBehavior AutoScrollMode="ToBottom" />
                    </Interaction.Behaviors>

                </ListBox>
            </Grid>

        </Expander>
        <ProgressBar
            Grid.Row="2"
            Height="10"
            IsIndeterminate="{Binding $self.IsVisible}"
            IsVisible="{Binding IsProcessing}"
            VerticalAlignment="Bottom" />
    </Grid>

</Window>
